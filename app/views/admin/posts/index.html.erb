<table>
	<thead>
		<tr>
			<th>ID</th>
			<th>Title</th>
			<th>Posted</th>
		</tr>
	</thead>
	<tbody>
		<% @posts.each do |post| %>
		<tr>
			<td><%= post.id %></td>
			<td><%= post.title %></td>
			<td><%= post.created_at %></td>
		</tr>
		<% end %>
	</tbody>
</table>
